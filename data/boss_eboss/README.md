# BAO + cronómetros cósmicos

Ficheros generados por `python scripts/download_data.py boss_eboss`
(tablas compiladas de la literatura, referencias en cada cabecera):

- `hz_cc.txt` — H(z) de cronómetros cósmicos (31 puntos).
- `bao_dr12.txt` — consenso BOSS DR12 (D_M/r_d, D_H/r_d).

Citar las referencias listadas en la cabecera de cada fichero.
